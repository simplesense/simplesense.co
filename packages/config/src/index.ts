/**
 * @ss/config — shared configuration: signal thresholds, subscription tiers, env access.
 * Values only (plus env reading); no domain computation (that lives in @ss/core).
 */
export * from './thresholds'
export * from './tiers'
export * from './env'
